---
export interface Props {
  placeholder: string | undefined;
  type?: 'text' | 'password' | 'email';
}

const { placeholder, type = "text" } = Astro.props as Props;
---

<input type={type} placeholder={placeholder} class="border border-recanndy-gray-50 py-3 px-4 rounded-sm text-sm" />