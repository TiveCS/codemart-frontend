---
export interface Props {
  placeholder: string | undefined;
  type?: 'text' | 'password' | 'email';
}

const { placeholder, type = "text" } = Astro.props as Props;
---

<input type={type} placeholder={placeholder} class="border py-2 px-4 " />